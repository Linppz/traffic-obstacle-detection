[pytest]
markers =
    requires_model: test needs the exported ONNX model under infer/models/
testpaths = app/tests
addopts = -ra -q
filterwarnings =
    ignore::DeprecationWarning:httpx
    ignore::DeprecationWarning:starlette
